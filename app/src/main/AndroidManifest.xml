<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shivamgupta.hisabkitab">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".MainPage" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".DonationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MoneyDonated" />
        <activity android:name=".DonateItemFromListActivity" />
        <activity android:name=".ConfirmDonationOrderActivity" />
        <activity android:name=".PaymentGateWayActivity" />
        <activity android:name=".ThankYouActivity" />
        <activity android:name=".OTPActivity"></activity>
    </application>

</manifest>